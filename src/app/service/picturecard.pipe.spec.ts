import { PicturecardPipe } from './picturecard.pipe';

describe('PicturecardPipe', () => {
  it('create an instance', () => {
    const pipe = new PicturecardPipe();
    expect(pipe).toBeTruthy();
  });
});
